/*module.exports = (sequelize, Sequelize) => {
  const Admin = sequelize.define("roles", {
    id: {
      type: Sequelize.INTEGER,
      primaryKey: true
    },
    username: {type: Sequelize.STRING}
  });

  return Role;
};*/
