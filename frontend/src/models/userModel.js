export default class User {
    constructor(username, email, password, service, bio, face) { 
      this.username = username;
      this.email = email;
      this.password = password;
      this.service = service;
      this.bio = bio;
      this.face = face;
    }
  }